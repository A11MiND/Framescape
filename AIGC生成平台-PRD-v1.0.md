# AIGC 内容生成平台 · 产品需求文档 v1.0

## 0. 这次改版是什么

`AIGC生成平台-POC-PRD-v0.2 (1).md`（以下简称 v0.2）是立项前的可行性论证文档：要不要用 Aether 做编排层、抄 PolyBuzz/即梦哪些交互、六道风险闸门怎么设。这些决策早就做完了，代码已经按这份文档跑了两个多月，期间又长出了 v0.2 完全没提过的东西——社区、连续发布积分奖励、Google/手机号/邮箱验证码登录、回收站、叙事连续性生成。继续拿 v0.2 当参照意义不大了。

v1.0 不是改几个章节，是重写：只写"这个产品现在实际是什么"，域模型、功能清单、数据结构、API、能力矩阵，每一条都对照当前代码库核实过（核实方法见文末）。竞品拆解、Aether 该不该用这类已经翻篇的决策叙事全部不再保留，需要时查 git 历史里的 v0.2 原文即可。

**两个直接后果，先说清楚：**
- 代码里大量注释写着"PRD §12.3""见 §19.0②"，那些章节号对应的是 v0.2 的结构。这次重写后编号全变了，这些注释会暂时指向一个不存在的旧结构——这是真重写必然的代价，不在这次改动范围内逐条清理，之后有空再排查替换。
- `DEV_PLAN.md`（周度进度追踪）已经有 5 个以上的功能提交没跟着更新（社区、回收站、跨镜头引用、视频 r2va 锚点、叙事连续性、image.single/batch 合并、四格漫画可变格数、作业软删除，以及这次会话做的社区连续发布/登录方式/图标）。v1.0 不再假设 DEV_PLAN.md 是准的。

---

## 1. 产品是什么

一句话生成一张图、一组连续镜头、一支短片——文字描述进去，画面出来，不用打开专业剪辑软件。

**六种生成形态**（这是全产品唯一的核心心智模型，其他一切功能都是围绕它转的）：

| 形态 | workflow_name | 一句话 |
|---|---|---|
| 单图 | `image.single` | 一句话出 1~9 张图（原来的"批量出图"已经合并进来，不再是独立形态） |
| 连续图 | `image.sequence` | 多句话出多张有关联的图，可以互相引用（"参考第2格") |
| 四格漫画 | `image.comic4` | 手写 2~9 格剧情，或者写一段故事自动拆格，自动衔接前后格画面 |
| 单段影片 | `video.single` | 文生视频 / 图生视频，一次出一段 |
| 连续影片 | `video.sequence` | 多段镜头，先出 768P 预览、勾选满意的几段升 2K 定稿，省钱 |

目标用户：不会用专业创作工具、但需要产出短视频/条漫/单图素材的人——自媒体、小型团队、个人创作者。定位不是"又一个 AI 绘图工具"，卖点是"连续影片的预览-定稿两段式"和"角色/风格能在多张图之间保持一致"，这两点在同类产品里少见。

---

## 2. 核心概念

理解这四个概念，就理解了这个产品的全部复杂度所在。

### 2.1 角色（Character）与预设（Preset）——一致性从哪来

生成 AI 图片/视频最大的痛点是"这一张"和"下一张"长得不像同一个人。这个产品用两个机制解决：

- **角色**：1~3 张参考图 + 一个固定 seed。绑定角色后，同一批生成会复用这个 seed（除非显式覆盖），参考图会作为 image-to-image 的输入。
- **预设**：一段可复用的 prompt 片段（风格、镜头语言等），按 `priority` 排序拼进最终 prompt，超字数从优先级最低的开始丢弃。系统自带的预设所有人可见；用户也可以"另存为我的预设"，只有自己能看到和删除——v0.2 原文把这个功能明确列为"永不做"的 P2，实际还是上线了。

编译逻辑（`internal/domain/prompt/compiler.go`）：角色描述和用户原文永远不裁剪（身份锚点，P0），预设按优先级从高到低往里加，超限就从最低优先级开始丢。Seed 优先级：显式传入 > 绑定角色的固定 seed > 不传（让 MiniMax 自己选）。

### 2.2 连续影片的预览门（Preview Gate）——全站最重要的单一交互

连续影片如果每一镜都直接出 2K，一次生成失败可能就是几十块钱打水漂。所以流程是两段式：

1. **草稿阶段**：所有镜头先以 768P 生成（成本 ¥0.50/秒，2K 是 ¥0.80/秒），生成完自动挂起（Aether 的 `human.gate` 节点）。
2. **预览门**：用户看这批 768P 结果，对每一镜选"保留 / 重做(仍 768P) / 升级(2K)"。只有选升级的镜头才需要二次扣费预留；重做的镜头 768P 阶段的预留额度已经够用。
3. **定稿**：`POST /jobs/{bizID}/resume` 把这个决策传回去，重做和升级各走一遍对应的 loop，最后拼接成片。

MiniMax 没有"放大"这个原语，所谓"升级到 2K"其实是重新按 2K 生成一遍，不是把 768P 的结果放大。

有个"跳过预览"开关（`skip_preview`）：直接按 2K 生成、挂起后系统自动帮你确认定稿，省一步交互，代价是失去了"先看 768P 效果再决定要不要多花钱"这层保护。

### 2.3 镜头怎么衔接——从"接上一帧"到"接住整段剧情"

连续影片/连续图默认是怎么让镜头之间连贯的：

- 第 1 镜（以及往后每隔 `recalibrate_every`=3 镜）是"锚点镜头"，用参考图/参考视频重新定调（r2va，没有参考就退化成纯文生视频 t2va）。
- 其余镜头默认续接上一镜的尾帧（i2va）。

再往上一层是**叙事连续性**（`narrative_continuity`，可选开关）：锚点镜头不再只接上一镜的一张尾帧，而是把最近生成的镜头（最多 3 段、总时长 ≤15 秒——这是 MiniMax r2va 参考视频的真实预算上限）连同关键帧、主角参考图，一起喂给 H3-Context-IR 做一次 prompt 改写，再拿去生成。哪几镜该被引用有三种模式：`window`（默认，最近若干镜滑动窗口）、`manual`（只认用户用"#"手动点出的镜头）、`smart`（用 MiniMax-M3 读全部镜头文案自己判断该引用谁，失败自动退回滑动窗口）。

### 2.4 积分：先扣后结算

提交生成时先按预估成本冻结积分（`held`），每个节点实际完成后按真实成本结算（`held`→ 真正扣除），作业整体结束后没用完的预留全额退回。文本节点（H3-Context-IR、MiniMax-M3 剧情拆分/智能引用）的成本先按保守上限预留，真实成本从供应商返回的 token 用量结算。

不变式：`可用余额 + 冻结中 == SUM(积分流水.amount)`。预留/退款两个动作本身不改变这个和（只是余额和冻结之间挪动），只有真正的扣费/充值才会改变这个和——这是为什么两套动作在流水表里记录的口径不一样，细节见 §7.3。

---

## 3. 功能清单

按模块列出，✅ = 已上线并有真实调用验证，⚠️ = 上线但有已知限制，标注见备注列。

### F1 账号与积分
- F1.1 邮箱+密码注册/登录 ✅
- F1.2 匿名试用（首页 `/` 不登录也能进，试用一次单图生成）✅
- F1.3 积分余额/冻结/流水 ✅
- F1.4 演示充值（非真实支付）✅
- F1.5 Google 登录（真实 ID Token 校验、找不到账号自动建号、按邮箱自动关联已有账号）⚠️ 代码完整但生产环境还没配 `GOOGLE_CLIENT_ID`
- F1.6 手机号登录（验证码即注册即登录，不需要单独的注册步骤）⚠️ 同上，还没接短信服务商
- F1.7 邮箱验证码（注册时可选二次校验）⚠️ 同上，还没接邮件服务商

### F2 资产库
- F2.1 预签名直传 ✅
- F2.2 生成结果自动落库（MiniMax 返回的 URL 是临时的，执行器落库时立即下载存到自己的对象存储）✅
- F2.3 供应商文件缓存（同一素材重复作为参考图使用时不用重新上传给 MiniMax）✅
- F2.4 资产库瀑布流：搜索、按项目/类型筛选 ✅
- F2.5 详情页：生成参数、"以此再生成" ✅
- F2.6 视频抽首尾帧存为角色参考图 ✅
- F2.7 软删除 + 30 天自动清空回收站 + 一键清空 + 批量下载 ✅

### F3 角色库
- F3.1 角色 CRUD（1~3 张参考图 + 固定 seed）✅
- F3.2 角色自动透传进生成（含视频：绑定角色但没传参考图时，自动用角色自己的参考图）✅

### F4 预设库
- F4.1~F4.4 系统预设浏览、按优先级拼接、超限裁剪 ✅
- F4.5 用户自建预设 ✅ ——v0.2 原文列为"永不做"的唯一一条 P2，实际已上线

### F5 图片生成
- F5.1/F5.2 单图 + 批量（`n=1..9`，已合并为一个 workflow，不再是两个）✅
- F5.3 四格漫画：手写模式（2~9 格）+ 剧情自动拆格模式（MiniMax-M3）✅，每一格生成前都先过一遍 H3-Context-IR 改写，格与格之间自动带上前几格的剧情大纲，不存在"快速模式跳过改写"这条路径了
- F5.4 剧情自动拆分格数 ✅
- F5.5 连续图（真正的 DAG，没有互相引用的镜头并行跑，只有引用了别的镜头的才等）✅
- F5.6 跨镜头引用（"#2" 语法，只能往前引用）✅
- F5.7 连续图两种模式：quick（默认不关联）/ continuity（默认接上一镜）✅
- F5.8 图生图（`source_image_asset_id`，六种形态全部支持）✅

### F6 视频生成
- F6.1 单段：文生视频/图生视频，首尾帧模式 ✅
- F6.2 三段式执行（提交→等待[回调优先+轮询兜底]→落库）✅，真实调用验证过
- F6.3 首尾帧模式底层机制存在且在连续影片里被使用，但没有单独针对"仅首尾帧"这条路径做过独立验证 ⚠️
- F6.4 角色自动透传到视频参考 ✅
- F6.5 模式互斥（首尾帧 vs 参考图/视频/音频不能同时传）前后端双重校验 ✅
- F6.6 连续影片：768P 预览 → 挂起 → 保留/重做/升级 2K → 拼接 ✅ 真实 3 镜头验证过
- F6.7 跳过预览（直接 2K，自动确认定稿）✅
- F6.8 每 N 镜重新锚定（r2va），N 可配置 ✅
- F6.9 叙事连续性（多镜头+关键帧+主角参考图打包送去改写）✅
- F6.10 AI Prompt 改写（H3-Context-IR，视频单段可选开关）✅
- F6.11 智能引用模式（MiniMax-M3 自主判断每镜该引用谁）✅，失败自动退化为窗口模式

### F7 作业管理
- F7.1 历史列表 ✅
- F7.2 DAG 可视化 ❌ **未做** —— `@xyflow/react` 依赖装在 `package.json` 里但代码里一次都没 import 过，详情页目前只有状态文字+进度条+结果网格，不是交互式图。这是真实缺口，不是文档没更新。
- F7.3 SSE 实时状态推送 ✅ 机制已建好，但延迟指标（要求 <2s）只在最早期验证过一次，后续几轮功能没有重新测过，判定低风险未复测
- F7.4 取消作业 ✅
- F7.5 单节点重试（"卫星 Job"重提交模式）✅
- F7.6 作业软删除 ✅（这次会话新增，仅终态作业可删）
- F7.7 Worker 崩溃恢复：kill -9 不丢不重 ✅ 真实验证（mock 执行器 + asynq 自动重投递）

### F8 内容安全
- F8.1/F8.2 生成时过滤（MiniMax 自己拒绝，前后端识别 `sensitive_content:` 前缀，不回显具体触发词）✅
- F8.3 生成后二次复核（每张图生成后用 MiniMax-M3 视觉输入单独查一遍，抓生成时过滤没拦住的东西，比如已知版权角色）✅ 真实验证过：向日葵图正常通过、皮卡丘图被正确标记"知名版权角色"。异步跑，不阻塞不影响作业本身成败。仅图片，视频没做（需要抽帧或视频输入模型）
- F8.4 审核记录留痕（`moderation_records` 表）✅
- **`assets.moderation_status` 列是死的** ⚠️——每条资产写入时都是 `"pending"`，从来没有任何地方读取或推进它。真正在工作的内容安全机制是 F8.1-F8.4，跟这一列无关，这列本身建议直接删掉而不是留着看起来像"有用但没做完"。

### F9 社区（本次会话新增）
- F9.1 发布/撤销发布到社区 ✅（撤销发布时的两个 UI bug 这次修了：204/200 状态码不一致导致前端误报失败；亮色主题下角标文字对比度问题）
- F9.2 社区 feed 浏览，游客也能看（本次会话放开，之前要登录）✅
- F9.3 "我发布的"自筛选 + 内联撤销 ✅
- F9.4 连续发布积分奖励：连续 3/10/30 天各奖励 10/50/100 积分，3 天奖励每自然月最多 4 次（且必须真正中断过才算下一次达成）、10 天奖励每自然月最多 1 次、30 天不设上限 ✅

---

## 4. 技术架构

三个无状态 Go 进程 + Aether 编排引擎 + MySQL/Redis/MinIO，监控用 Prometheus/Grafana（docker-compose 里已经是真实跑起来的服务，不是规划中）。

```
┌─────────┐      ┌────────────┐      ┌─────────┐
│ cmd/api │─RPC─▶│cmd/scheduler│◀────▶│cmd/worker│
└────┬────┘      └──────┬─────┘      └────┬────┘
     │                  │                  │
     ▼                  ▼                  ▼
  MySQL              Redis              MinIO
  (业务表+Aether表)   (asynq队列+SSE)    (对象存储)
```

- **`cmd/api`**：纯 Gin HTTP 层，认证、校验、Job CRUD。从不持有 Aether Engine 实例，通过内部 RPC client 找 `cmd/scheduler` 要状态——这样 api 可以水平扩容。
- **`cmd/scheduler`**：唯一持有 Aether Engine 实例的进程（必须单实例，否则会重复推进同一个 DAG 的状态），同时跑 asynq 控制队列消费者和后台巡检（挂起超时自动取消退款、积分对账、跳过预览的自动确认定稿）。
- **`cmd/worker`**：执行器宿主，消费 asynq 派发队列，跑具体的 `executor.Plugin`（`minimax.image`/`minimax.video`/`local.compose`/`local.ffmpeg.*`/`mock.*` 等），完事向 scheduler 的控制队列回报。不直接碰 Aether 的 Store，也不持有 Engine——收到的是一个"胖"任务分配，所有需要的东西都在里面。
- 新增一个执行器只需要一个新文件 + `cmd/scheduler`/`cmd/worker` 各一行注册——这条架构特性反复被验证过（W5-W7 期间新增了 8 个真实执行器，全部符合这个模式）。

---

## 5. 数据模型

18 张表（`SHOW TABLES`，不含 Aether 自己的两张）：

| 表 | 用途 |
|---|---|
| `users` | email/phone/password_hash/google_sub 全部可空——只要求至少一种登录方式 |
| `credit_accounts` / `credit_ledger` | 余额/冻结 + 完整流水（见 §2.4 不变式） |
| `assets` | 素材主表：`is_public`/`published_at`（社区）、`moderation_status`（死列，见 F8）、`project_id`、`deleted_at`（软删除） |
| `jobs` / `job_nodes` | 作业主表（`credit_held`/`credit_settled`/`credit_estimated`/`idem_key`/`deleted_at`）+ 节点明细 |
| `characters` / `presets` / `projects` | F3/F4 + 资产分组 |
| `provider_files` | MiniMax 文件 ID 缓存（避免重复上传） |
| `moderation_records` | F8.4 审核留痕 |
| `community_streaks` / `community_streak_rewards` | 连续发布：跑动计数器 + 每月奖励发放审计（不含逐日历史，见 §9 已知缺口） |
| `phone_verification_codes` / `email_verification_codes` | 验证码存储（本次会话新增） |
| `aether_task_runs` / `aether_workflow_runs` | Aether 自己的编排状态，不直接读写 |
| `goose_db_version` | 迁移版本追踪，14 个迁移文件 |

---

## 6. API 设计

`/api/v1` 前缀省略。公开路由不需要 JWT，其余都要 `Authorization: Bearer`。

**公开**
```
POST /auth/register /auth/login /auth/refresh
POST /auth/google /auth/phone/send-code /auth/phone/verify /auth/email/send-code
POST /trial/image
GET  /capabilities
GET  /community/feed
```

**需要登录**
```
账号   GET/PATCH /me, /me/password
作业   POST /jobs · GET /jobs · POST /jobs/estimate · GET /jobs/:bizID
       GET /jobs/:bizID/events (SSE) · POST /jobs/:bizID/resume · POST /jobs/:bizID/cancel
       DELETE /jobs/:bizID · POST /jobs/:bizID/nodes/:nodeName/retry
资产   POST /assets/upload-url · POST /assets/:bizID/complete
       GET /assets · GET/PATCH/DELETE /assets/:bizID
       GET /assets/trash · POST /assets/:bizID/restore · POST /assets/trash/empty
       POST /assets/batch-download
社区   GET /community/streak
角色   POST/GET /characters · PATCH/DELETE /characters/:bizID
预设   GET/POST /presets · DELETE /presets/:bizID
积分   GET /credits/balance · GET /credits/ledger · POST /credits/topup
项目   POST/GET /projects · PATCH/DELETE /projects/:bizID
提示词 POST /prompts/rewrite
```

不挂 `/api/v1` 前缀的两个特殊路由：`POST /internal/callbacks/minimax`（MiniMax 视频状态回调）、`GET /internal/health` `/metrics`。

---

## 7. 能力矩阵与业务规则

### 7.1 图片（MiniMax image-01）
- 单次批量上限 `n=9`，同时也是四格漫画的格数上限
- prompt 上限 1500 字

### 7.2 视频（MiniMax-H3）
- 时长 4~15 秒，prompt 上限 7000 字
- 分辨率：768P / 2K；比例：21:9 / 16:9 / 4:3 / 1:1 / 3:4 / 9:16（`adaptive` 不对用户开放，只在 i2va 模式下系统自动强制）
- **模式互斥**：首尾帧模式（i2va）不能和任何参考图/视频/音频（r2va）同时传，否则请求前直接拒绝、零成本报错
- **模式判定**：都没传→t2va（纯文生视频，ratio 必填且不能是 adaptive）；传了首尾帧→i2va（ratio 强制 adaptive）；传了参考→r2va（ratio 可选，默认 adaptive）

以上规则由 `capability` 包提供边界常量，互斥/条件规则本身写死在执行器里（`minimax/video.go`），不是通过配置驱动的——这是当前的实际实现方式，跟 v0.2 原本设想的"能力矩阵读一张完整的结构体表"有出入，值得后续考虑要不要收敛回配置化。

### 7.3 积分定价
```
图片: ¥0.025 / 张
视频: 768P ¥0.50/秒，2K ¥0.80/秒
积分 = ceil(成本 × 2.0 / 0.10)，向上取整，成本>0 时最低 1 积分
```
四格漫画/连续图这类"一个作业里有 N 个独立计费节点"的形态，每个节点各自向上取整再求和，不能按合并总成本算一次取整（4 张 ¥0.025 各自取整是 4 积分，合并算是 2 积分——用错口径是生产环境真实出现过的对账 bug）。

### 7.4 连续发布积分奖励
连续 3/10/30 天发布素材到社区，各奖励 10/50/100 积分。3 天奖励每自然月最多拿 4 次，且必须真正中断过才算下一次新达成（不间断发布 30 天只会在第 3 天拿一次，不会每 3 天重复拿）；10 天奖励每自然月最多 1 次；30 天不设上限。

---

## 8. 前端信息架构

| 路由 | 是否需要登录 | 页面 |
|---|---|---|
| `/login` | — | 邮箱/Google/手机号三种方式，含语言/主题切换（这个页面不挂载 AppShell，之前一直没有这两个切换） |
| `/`（首页=创作台） | 游客可进 | **Studio.tsx**，全站最核心最大的页面 |
| `/characters` `/presets` `/assets` `/assets/:id` | 需要 | F3/F4/F2 对应管理页 |
| `/community` | 游客可看 feed，"我发布的"和积分面板需要登录 | 社区 |
| `/jobs` `/jobs/:id` | 需要 | 作业历史/详情 |
| `/credits` `/projects` `/settings` | 需要 | 积分流水、项目管理、账号设置 |

**创作台（Studio.tsx）不是六个 tab，是一个统一输入框**：一套快切卡片 + 一个 `tab` 状态驱动 `image.single | image.comic4 | video.single | video.sequence` 四种入口（`image.sequence` 没有独立卡片，是 `image.single` 里的"连续多段"子开关，但在作业历史/费用明细里仍然是独立的类型标签）。首页游客可进——早期版本 `/` 是直接跳登录页的，把匿名试用整个埋没了，这是信息架构上修过的最大一条问题。

组成部件：`#`提及式跨镜头/角色引用输入框、角色槽选择器、预设轮播、首页三 tab 信息流（最近产物/灵感范例/预设风格）、拖拽排序镜头列表、SSE 驱动的实时进度、预览门决策组件、AI 改写按钮、"建议下一步"操作条。

---

## 9. 已知缺口（诚实列出，不回避）

1. **F7.2 DAG 可视化没做**——依赖装了，代码没写，作业详情页目前只有文字状态。
2. **`assets.moderation_status` 是个死列**——建议直接删掉，容易被误读成"内容审核没做"，实际 F8 系列是有真实机制在跑的。
3. **SSE <2s 延迟指标只验证过一次**，之后几轮功能迭代没有重新测过。
4. **`DEV_PLAN.md` 落后 5+ 个功能提交**，不能再当作最新进度的准确来源。
5. **Scheduler 只能单实例**，水平扩容需要先做 leader 选举，目前完全没做。
6. **Google 登录 / 手机号验证码 / 邮箱验证码三条认证路径代码已完整，但生产密钥都还没配**（`GOOGLE_CLIENT_ID` / `SMS_API_KEY` / `EMAIL_PROVIDER_API_KEY`），配置好之前这三条路径对用户可见但不可用，会提示"尚未开放"。
7. **PromptCompiler 的"参考图角色映射"和"互斥校验"两步没有做成编译器内部步骤**，现在是按生成形态各自 ad-hoc 处理，逻辑分散在好几个地方。
8. **F6.3（纯首尾帧模式）没有独立验证过**，底层机制在连续影片里天天在用，但单独拎出来测试的记录没有。
9. **社区连续发布只有"当前连续天数"这个滚动计数器，没有完整的逐日历史记录**——本次会话做过一版日历可视化，验证方向后又因为产品判断（想要的是极简样式，不是日历）撤掉了，连带支撑它的表也删了，如果以后想做历史回看需要重新加。

---

## 附录：与 v0.2 的关系

- v0.2 里"1. 竞品与技术选型反推""2. Aether 采纳决策"这类立项期决策叙事，决策结论已经固化进代码两个多月，v1.0 不再重复，需要时查 git log 里的 v0.2 原文。
- v0.2 §10.5 设想的"能力矩阵是一张完整结构体"，实际实现是常量 + 硬编码规则分开放（§7 已说明），这是当时没预料到的实现路径分歧，v1.0 按实际情况记录，不按原计划记录。
- v0.2 明确列为"P2 永不做"的 F4.5（用户自建预设）已经上线，v1.0 按实际状态记录为 F4 的一部分。
- 本文档所有事实性描述均对照当前代码库、数据库表结构、真实路由表核实，核实方式：读取 `workflows/*.json`、`internal/domain/capability`、`internal/application/jobsvc`、`internal/infra/executor/minimax`、`internal/interfaces/http/server.go`、`web/src/App.tsx`、`docker exec ... SHOW TABLES`、`DEV_PLAN.md` §13/§7 原文，2026-08-19。
